%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Chapter 4: Methods — characterising spectra with MCMC
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Methods: characterising spectra using \texttt{asap}}
\label{sec:methods}
% This chapter describes how we recover atmospheric and magnetic parameters from the template spectra of Sect.~\ref{sec:obs_preparation}. We use \texttt{asap}, a retrieval pipeline that couples a forward model, which turns a parameter set into a synthetic spectrum, with an MCMC sampler, which explores the parameter space and scores each proposal by how well its spectrum reproduces the observations. Run as a loop, the two map out the posterior distribution of the parameters, from which we report point estimates (posterior medians) and credible intervals. The parameters are the effective temperature $T_\mathrm{eff}$, surface gravity $\log g$, metallicity $\mathrm{[M/H]}$, radial velocity $v_r$, macroturbulence $\zeta_\mathrm{RT}$, and a set of magnetic filling factors $f_i$ defined on a grid of field strengths $B_i$ spanning $0$--$10\,\mathrm{kG}$.

% This chapter describes how we recover atmospheric and magnetic parameters from the template spectra of Sect.~\ref{sec:obs_preparation}. We use \texttt{asap}, a spectral analysis pipeline that pairs a forward model --- which turns a set of stellar parameters into a synthetic spectrum --- with an MCMC sampler that explores the parameter space to find the values whose spectrum best reproduces the observations. These parameters are the effective temperature $T_\mathrm{eff}$, surface gravity $\log g$, metallicity $\mathrm{[M/H]}$, radial velocity $v_r$, macroturbulence $\zeta_\mathrm{RT}$, and a set of magnetic filling factors $f_i$ defined on a grid of field strengths $B_i$ spanning $0$--$10\,\mathrm{kG}$.

This chapter describes how we recover atmospheric and magnetic parameters from the template spectra of Sect.~\ref{sec:obs_preparation}. A forward model takes a set of parameters and predicts the spectrum they would produce. The reverse --- recovering the parameters from an observed spectrum --- cannot be done directly. Instead, the parameters are inferred by searching for the set whose predicted spectrum best matches the observation, a process known as retrieval. We carry this out with \texttt{asap}\footnote{\url{https://github.com/pcristof/asap.git}}, a pipeline that pairs such a forward model with an MCMC sampler. The parameters inferred are the effective temperature $T_\mathrm{eff}$, surface gravity $\log g$, metallicity $\mathrm{[M/H]}$, radial velocity $v_r$, macroturbulence $\zeta_\mathrm{RT}$, and a set of magnetic filling factors $f_i$ defined on a grid of field strengths $B_i$ spanning $0$--$10\,\mathrm{kG}$.

% The chapter follows the pipeline from a single fit to the full sample. Sect.~\ref{sec:methods_fit} sets out the \texttt{asap} retrieval itself: the forward model, how magnetic activity enters it, the free parameters and priors, and the likelihood. Sect.~\ref{sec:methods_linelist} describes the common atomic and molecular line list, which selects the spectral regions that enter the likelihood. Sect.~\ref{sec:methods_filter} adds a per-star, $\chi^2$-based filter that prunes regions the forward model fits poorly, so a single starting list can serve a diverse sample. Sect.~\ref{sec:methods_bic} introduces a post-hoc BIC scan that fixes the number of magnetic components per star, in place of the fixed magnetic grids used by \citet{cristofari+2022b, cristofari_2023b, reiners+2022}.

% Sect.~\ref{sec:methods_fit} introduces the single-star fit. Sect.~\ref{sec:methods_linelist} describes the atomic and molecular line list common to all stars, drawn from the VALD3 database [CITATION NEEDED: vald3]. Sect.~\ref{sec:methods_filter} introduces a per-star, $\chi^2$-based filter that prunes spectral regions in which the forward model fits the data poorly, allowing the use of a uniform initial list across a heterogeneous sample. Sect.~\ref{sec:methods_bic} introduces a post-hoc $\mathrm{BIC}$ scan that selects the number of magnetic filling-factor components per star, in contrast to the fixed magnetic grids used by \citet{cristofari+2022b, cristofari_2023b, reiners_2022}. Sect.~\ref{sec:methods_campaign} closes by describing how these elements are assembled into a three-phase campaign that produces the self-consistent parameter set used in the chapters that follow.


\subsection{The \texttt{asap} retrieval}
\label{sec:methods_fit}
The retrieval operates iteratively. At each step the sampler proposes a parameter set, the forward model renders the corresponding synthetic spectrum, and the likelihood measures its agreement with the observation, so the proposal is accepted or rejected accordingly. This step is repeated many times, and the accepted positions trace out the posterior. We use the affine-invariant ensemble sampler \texttt{emcee}\footnote{\url{https://emcee.readthedocs.io/en/stable/}} \citep{emcee}.

% The retrieval proceeds iteratively. At each step the sampler proposes a new parameter set,
% the forward model renders it, and the likelihood scores its agreement with the
% observation, so the proposal is accepted or rejected accordingly. Over many steps the
% accepted positions trace out the posterior. We use the affine-invariant ensemble sampler
% \texttt{emcee}\footnote{\url{https://emcee.readthedocs.io/en/stable/}} \citep{emcee}.

% The remainder of this section follows that division. Sect.~\ref{sec:methods_forward} and Sect.~\ref{sec:methods_magnetic} describe the forward model and how magnetic activity enters it. Sect.~\ref{sec:methods_freeparams} lists the sampled parameters and their priors and Sect.~\ref{sec:methods_likelihood} the likelihood and error treatment.

\subsubsection{Forward model}
\label{sec:methods_forward}
% Synthetic spectra are pulled from the precomputed \texttt{ZeeTurbo} grid and linearly interpolated over the three atmospheric parameters $(T_\mathrm{eff}, \log g, \mathrm{[M/H]})$ at each magnetic component $B_i$ spanning $0$--$10\,\mathrm{kG}$ in $1\,\mathrm{kG}$ steps. 
The forward model begins by linearly interpolating within the precomputed \texttt{ZeeTurbo} grid over the three atmospheric parameters $(T_\mathrm{eff}, \log g, \mathrm{[M/H]})$ at each magnetic component $B_i$, spanning $0$--$10\,\mathrm{kG}$ in $1\,\mathrm{kG}$ steps. This produces 11 spectra $S_i(\lambda)$, one per field strength, which the magnetic model of Sect.~\ref{sec:methods_magnetic} combines into the single spectrum $S_\mathrm{mag}$ used in the rest of the forward model (Eq.~\ref{eq:magnetic-mixing}). Figure~\ref{fig:forward_model} traces this construction for a single Zeeman-sensitive Ti\,\textsc{i} line in Gl~388 (AD~Leo). The Zeeman broadening grows visibly with $B_i$ across these spectra.

The combined spectrum is then shifted to the stellar rest frame using the relativistic Doppler factor for a radial velocity $v_r$, whose initial value is obtained once before sampling by cross-correlating the observed spectrum against a VALD\footnote{\url{https://vald.astro.uu.se/}} absorption-line mask. The spectrum is then broadened by convolution with a kernel combining the instrumental profile (a Gaussian of FWHM $4.6\,\mathrm{km\,s^{-1}}$ for NARVAL/ESPaDOnS), radial-tangential macroturbulence ($\zeta_\mathrm{RT}$), and rotational broadening (the Gray rotation profile, \citealt{gray_1992}). Rotation is neglected here, with $v\sin i$ fixed to zero, as justified by the long rotation periods measured for these stars \citep{fouque_2023, Donati_2023}, which place their equatorial velocities well below the instrumental profile across the sample. Because rotation is fixed to zero, the fitted $\zeta_\mathrm{RT}$ measures macroturbulence alone rather than a combination of the two effects. The fully transformed synthetic spectrum is then resampled onto a reference wavelength grid.
% We write the resulting model flux at pixel $i$ as $M_i$, the quantity that enters the likelihood of Sect.~\ref{sec:methods_likelihood}.

% which is defined as follows:
% \begin{equation}
%     \mathcal{D}(v_r) \;=\; \sqrt{\frac{1 + \beta}{1 - \beta}} , \qquad \beta \equiv \frac{v_r}{c}.
% \end{equation}
% Initially, this $v_r$ is found by cross-correlation the observed spectrum $S(\lambda)$ against a VALD absorption line-mask $\{(\lambda_j^{(0)},\, w_j)\}$, where $\lambda_j^{(0)}$ are rest-frame line centres and $w_j$ are the depths of the lines, which are used as line weights. With median-subtracted versions $\tilde w_j = w_j - \operatorname{med}(w)$ and $\tilde S = S - \operatorname{med}(S)$, the normalised CCF at velocity $v$ is
% \begin{equation}
%     \rho(v) \;=\; \frac{\sum_j \tilde w_j \, \tilde S\!\bigl(\lambda_j^{(0)} \mathcal{D}(-v)\bigr)}{\sqrt{\,\sum_j \tilde w_j^{\,2} \;\; \sum_j \tilde S\!\bigl(\lambda_j^{(0)} \mathcal{D}(-v)\bigr)^{2}\,}} .
% \end{equation}
% The initial RV values is calculated in two passes; a coarse and a fine pass:
% \begin{enumerate}
%     \item \textbf{Coarse}: $v \in \{-250,\dots,+250\}$ km s$^{-1}$ at $\Delta v = 2$ km s$^{-1}$. $v_0$ is then the value for which $\rho$ is minimal.
%     \item \textbf{Fine}: Pre-shift the mask by $\mathcal{D}(-v_0)$ and then recompute $\rho(v)$ for narrower set of velocities of $\pm 25$ km s$^{-1}$ at $\Delta v = 0.1$ km s$^{-1}$, then cubic-interpolate $\rho$ onto a 0.01 km s$^{-1}$ grid and locate $v_1$.
% \end{enumerate}
% The returned initial Radial velocity is then equal to $v_\text{RV} =  v_0 + v_1$. This value is then used to shift the wavelength solution of the synthetic spectrum to match the observed radial velocity.

\subsubsection{Modelling magnetic intensity}
\label{sec:methods_magnetic}
% Following \citet{shulyak+2010, shulyak+2014, kochukhov+2020, reiners+2022, cristofari_2023a}, the stellar spectrum is modelled as a linear combination of the spectra computed with \texttt{ZeeTurbo} for various magnetic field strengths. These interpolated spectra are linearly combined in flux, weighted by the filling factors $f_i$,
% \begin{equation}
%   \label{eq:magnetic-mixing}
%   S_\mathrm{mag}(\lambda) = \sum_{i=0}^{N_B} f_i \, S_i(\lambda),
%   \qquad \sum_{i=0}^{N_B} f_i = 1,
%   \qquad f_i \geq 0 ,
% \end{equation} 
% where each weight $f_i$ is the fraction of the disc-integrated flux emitted by regions at field strength $B_i$. The two constraints have direct meaning: $f_i \geq 0$ because a region cannot cover a negative fraction of the disc, and $\sum_i f_i = 1$ because the strengths must account for the whole visible surface, with $f_0$ the non-magnetic fraction. Together they make $\{f_i\}$ a normalised distribution over field strength, which can be seen as a histogram, binned at the grid resolution, of how the visible disc is partitioned among the available strengths.

% The MCMC does not sample all $N_B + 1$ weights independently. It samples the $N_B$ magnetic factors $f_1, \dots, f_{N_B}$ and fixes the non-magnetic factor by closure, $f_0 = 1 - \sum_{i \geq 1} f_i$.

The linear combination introduced above follows the approach of \citet{shulyak+2010, shulyak+2014, kochukhov+2020, reiners+2022, cristofari_2023a}, in which each weight $f_i$ is the filling factor of field strength $B_i$ --- the fraction of the disc-integrated flux emitted by regions at that strength.
\begin{equation}
  \label{eq:magnetic-mixing}
  S_\mathrm{mag}(\lambda) = \sum_{i=0}^{N_B} f_i \, S_i(\lambda),
  \qquad \sum_{i=0}^{N_B} f_i = 1,
  \qquad f_i \geq 0 ,
\end{equation} 
The two constraints have direct meaning: $f_i \geq 0$ because a region cannot cover a negative fraction of the disc, and $\sum_i f_i = 1$ because the strengths must account for the whole visible surface, with $f_0$ the non-magnetic fraction. Together they make $\{f_i\}$ a normalised, discrete distribution over the grid of field strengths, describing how the visible disc is partitioned among the available strengths. The average magnetic field strength of the star is then computed by taking the sum of the field strengths weighted by their corresponding filling factor: $\langle B \rangle = \sum_i f_i B_i$.

The two constraints are enforced separately. Only the $N_B$ magnetic factors $f_1, \dots, f_{N_B}$ are free parameters. The non-magnetic factor is not sampled but set by closure when the spectrum is constructed, $f_0 = 1 - \sum_{i \geq 1} f_i$, which enforces the normalisation.

\subsubsection{Free parameters and priors}
\label{sec:methods_freeparams}
Every star in the sample is fitted with the same free parameters, all given uniform priors whose bounds are listed in Table~\ref{tab:mcmc_priors}. The bounds on the atmospheric parameters $T_\mathrm{eff}$, $\log g$ and $[\mathrm{M/H}]$ are inherited from the \texttt{ZeeTurbo} grid extents, and the filling factors $f_1,\dots,f_{N_B}$ are uniform over the simplex defined in Sect.~\ref{sec:methods_magnetic}. The number of magnetic components $N_B$ is not itself sampled but fixed for a given run, selected post-hoc by the BIC scan (Sect.~\ref{sec:methods_bic}). The remaining parameters supported by \texttt{asap} --- $v\sin i$, $[\alpha/\mathrm{Fe}]$, the veiling factor, and any additional Gaussian broadening --- are held fixed at zero across the sample.

\begin{table}
\caption{Uniform-prior bounds on the MCMC free parameters for the optical (NARVAL/ESPaDOnS) retrieval. The atmospheric-parameter bounds correspond to the extent of the \texttt{ZeeTurbo} grid. The macroturbulent kernel mode and the fixed instrumental broadening are not priors but are listed for completeness.}
\label{tab:mcmc_priors}
\centering
\begin{tabular}{lc}
\toprule
Parameter & Value \\
\midrule
$T_\mathrm{eff}$ [K]                       & $[3000,\,4000]$ \\
$\log g$ [dex]                              & $[4.5,\,5.5]$ \\
$[\mathrm{M/H}]$ [dex]                      & $[-0.5,\,+0.5]$ \\
$v_r$ [km\,s$^{-1}$]                        & $[-200,\,+200]$ \\
$\zeta_\mathrm{RT}$ [km\,s$^{-1}$]          & $\geq 0$ \\
$\{f_i\}$                                   & simplex \\
$\{B_i\}$ initial set [kG]                  & $\{0,1,\dots,10\}$ \\
\midrule
Macroturbulent kernel                       & radial-tangential \\
$v_\mathrm{instru}$ [km\,s$^{-1}$]          & $4.6$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}
    \centering
    \includegraphics[width=\linewidth]{figures/wave_explorer.png}
    \caption{The \texttt{wave\_explorer} tool applied to Gl~410 (DS~Leo). The main panel shows the observed spectrum (grey) and the model fit (red), with the residuals (obs $-$ fit) in the lower sub-panel. Selected regions are drawn as shaded vertical bands, colour-coded by their mean pixel-wise $\chi^2$. Hovering over a region displays its statistics. VALD lines can be overplotted to identify which species and transition each spectral feature corresponds to. Regions can be added, excluded, or have their bounds adjusted. Summary statistics for all regions are given in the lower-left panel, while the lower-right panel lists the worst-fitted regions, each of which can be clicked to zoom directly to that region.}
    \label{fig:wave_explorer}
\end{figure*}

\subsubsection{Likelihood and error estimation}
\label{sec:methods_likelihood}
Following \citet{cristofari_2023a, cristofari_2023b}, we adopt a pixel-wise Gaussian likelihood. With $O_i$ the observed normalised flux at pixel $i$, $M_i$ the synthetic flux returned by the forward model, and $\sigma_i$ the observational uncertainty on $O_i$, the chi-squared statistic is
\begin{equation}
    \label{eq:chi2}
    \chi^2 = \sum_{i=1}^{n} \frac{(O_i - M_i)^2}{\sigma_i^2} ,
\end{equation}
and the log-likelihood is
\begin{equation}
    \label{eq:likelihood}
    \ln \mathcal{L} = -\frac{1}{2}\chi^2 - \frac{1}{2}\sum_{i=1}^{n} \ln\!\left(2\pi\sigma_i^2\right) ,
\end{equation}
where $n$ is the number of fitted pixels.

To absorb modelling-induced systematics that the pipeline uncertainties do not capture, we follow the two-pass procedure of \citet{cristofari_2023a, cristofari_2023b}. A first MCMC is run with the original $\sigma_i$. The residuals at the posterior median yield a reduced chi-squared $\chi^2_\nu = \chi^2/\nu$, with $\nu = n - k$ degrees of freedom for $k$ free parameters. In the subsequent run, all pixel errors are inflated as $\sigma_i \to \sigma_i\sqrt{\chi^2_\nu}$, so that the best fit has $\chi^2_\nu \approx 1$.

Each parameter's reported value is taken as the mean of the highest-likelihood $5\%$ of samples, which approximates the maximum a posteriori (MAP) estimate rather than the posterior median. Its associated formal error bar is given by half the width of the central $68\%$ (16th--84th percentile) interval of the second-pass posterior. These formal bars are statistical lower bounds that reflect only the width of the posterior and do not capture systematics or model error, which is why the empirical floors of \citet{cristofari_2023a, cristofari_2023b} are adopted as the reported uncertainties. Typical formal values are $5$--$20$~K in $T_\mathrm{eff}$ and $0.005$--$0.080$~dex in $\log g$ and $[\mathrm{M/H}]$. \citet{cristofari+2022b} found these formal errors to be significantly smaller than the dispersion of derived parameters across reasonable modelling assumptions, and proposed \emph{empirical} error bars obtained by adding in quadrature a floor of $30$~K in $T_\mathrm{eff}$, $0.05$~dex in $\log g$, and $0.10$~dex in $[\mathrm{M/H}]$.

% \subsubsection{MCMC sampling}
% \label{sec:methods_sampling}
% The posterior is explored with \texttt{emcee}, an ensemble Markov-chain Monte Carlo sampler that uses the affine-invariant proposal of \citet{emcee_algorithm}. A population of $K$ walkers is initialised in the parameter space of Sect.~\ref{sec:methods_freeparams} and advanced jointly, each walker proposing a new position from the current configuration of the ensemble. At each proposal the forward model of Sect.~\ref{sec:methods_forward} is evaluated and compared to the observed flux through the likelihood of Sect.~\ref{sec:methods_likelihood}, and the move is accepted or rejected with a probability set by the ratio of posterior densities at the new and current points. The point estimates and error bars of Sect.~\ref{sec:methods_likelihood} are then computed from the accumulated walker positions.
\begin{table}
\caption{Initial optical line list. The identification relied on wavelengths and depths extracted from the VALD database. Each entry reports the central wavelength of the line region (vacuum \AA). Effective Landé factors are given for atomic lines.}
\label{tab:linelist_opt}
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}c@{\hspace{6pt}}>{\centering\arraybackslash}p{\dimexpr\linewidth-1.5cm\relax}@{}}
\toprule
Species & Wavelength (\AA) [$g_\mathrm{eff}$] \\
\midrule
Fe\,\textsc{i} & 7513.12 [1.42], 8049.86 [1.50], 8077.42 [0.76], 8222.67 [1.11], 8470.79 [2.49], 8516.35 [1.83], 8517.45 [0.78], 8584.60 [1.07], 8614.15 [1.49], 8623.90 [1.22], 8759.60 [1.49], 8807.06 [1.02], 8840.88 [1.50] \\
Mg\,\textsc{i} & 8809.15 \\
Ti\,\textsc{i} & 7472.04 [1.37], 8027.08 [1.06], 8070.45 [0.75], 8399.16 [0.00], 8419.26 [0.87], 8441.62 [1.12], 8677.72 [1.19], 8685.38 [0.99], 9691.50 [1.50], 9708.31 [1.26], 9721.64 [0.95], 9731.10 [1.00], 9772.98 [1.55], 9786.11 [1.48] \\
V\,\textsc{i} & 8119.00 [1.42] \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
  font=\small, text=black,
  >={Stealth[length=2.6mm,width=2mm]},
  % ---- shared geometry: every box lines up to the same grid ----
  box/.style={draw, align=center, inner sep=2pt, minimum height=9mm,
              line width=0.8pt, fill=white},
  card/.style={box, text width=28mm},                  % fixed-width step box
  term/.style={box, rounded corners=8pt, inner xsep=9pt, inner ysep=1pt, line width=0.9pt,
               draw=termAc, fill=termBg, font=\small\bfseries},  % start / end
  diag/.style={card, draw=diagAc},                     % diagnostic-phase step
  prod/.style={card, draw=prodAc},                     % production-phase step
  bridge/.style={card, rounded corners=4pt, draw=edge},% decision outcome
  dec/.style={draw=decAc, fill=decBg, diamond, aspect=2.4,
              align=center, inner sep=2pt, line width=0.8pt},     % decision
  arr/.style={->, draw=edge, line width=0.8pt},
  pno/.style={font=\footnotesize\bfseries, draw=noAc, text=noAc, fill=white,
              line width=0.7pt, inner sep=2.5pt, rounded corners=2pt},
  pyes/.style={font=\footnotesize\bfseries, draw=yesAc, text=yesAc, fill=white,
               line width=0.7pt, inner sep=2.5pt, rounded corners=2pt},
  banner/.style={font=\small\bfseries, text=white, inner sep=3.5pt,
                 rounded corners=3pt, anchor=west, draw=diagAc},                % phase tab
]

% ============================= start ===============================
\node[term] (A) {Star $+$ common line list};

% ========================= diagnostic spine ========================
\node[diag, below=8mm of A] (B) {Run pipeline with reduced steps};
\node[diag, below=6mm of B]  (C) {Compute $\chi^2_R/N_R$ for each region};
\node[dec,  below=5mm of C]  (D) {Any region with\\[1pt]$\chi^2_R/N_R > 50$\,?};

% ===================== branch (decision outcome) ===================
\node[diag] (E) at ([xshift=-22mm,yshift=-15mm]D.south) {Common list is final};
\node[diag] (F) at ([xshift= 22mm,yshift=-15mm]D.south) {Drop rejected regions\\$\rightarrow$ filtered list};

% ======================== production lanes =========================
\node[prod, below=12mm of E] (G) {Run to full length};
\node[prod, below=12mm of F] (H) {Rerun from scratch\\on filtered list};
\node[term] (I) at ($(G.south)!0.5!(H.south)+(0,-8mm)$) {Posterior estimates};

% ===================== phase frames (background) ===================
\coordinate[above=4mm of B] (diagTop);
\coordinate[below=21mm of D] (diagBot);
\coordinate[above=6mm of G] (prodTop);
\coordinate[below=3mm of I] (prodBot);
\begin{scope}[on background layer]
  \node[draw=diagAc, fill=diagBg, rounded corners=9pt, line width=1pt,
        fit=(B)(C)(D)(diagTop)(diagBot), inner xsep=23.5mm, inner ysep=0mm] (diagF) {};
  \node[draw=prodAc, fill=prodBg, rounded corners=9pt, line width=1pt,
        fit=(G)(H)(I)(prodTop)(prodBot), inner xsep=8mm, inner ysep=0mm] (prodF) {};
\end{scope}

% ============================= arrows ==============================
\draw[arr] (A) -- (B);
\draw[arr] (B) -- (C);
\draw[arr] (C) -- (D);

\draw[arr] (D.south) -- (E.north) node[pno, pos=0.5] {No};
\draw[arr] (D.south) -- (F.north) node[pyes, pos=0.5] {Yes};

\draw[arr] (E) -- (G);
\draw[arr] (F) -- (H);

\draw[arr] (G.south) |- (I.west);
\draw[arr] (H.south) |- (I.east);

% phase banners drawn LAST so they sit on top of the arrows; fill masks
% any arrow passing beneath, keeping the label text uncut
\node[banner, fill=diagAc] at ([xshift=7mm]diagF.north west) {Diagnostic phase};
\node[banner, fill=prodAc] at ([xshift=7mm]prodF.north west) {Production phase};

\end{tikzpicture}%
}
\caption{Per-star line-list filtering workflow. In the diagnostic phase (blue) the pipeline is run using the common line list with a reduced number of MCMC steps, and the mean per-pixel $\chi^2$ over the fitted pixels of each region, $\chi^2_R/N_R$, is evaluated. If no region exceeds the threshold the common list is final and the run is carried to its full production length. If any region does, those regions are filtered out and the pipeline is rerun from scratch with the filtered line list. The production phase (green) is in either case an ordinary pipeline run that differs only in the line list it uses, and it is this run that yields the posterior estimates.}
\label{fig:linelist_filter}
\end{figure}

\subsection{Line list}
\label{sec:methods_linelist}

% \begin{figure*}
%     \centering
%     \includegraphics[width=\linewidth]{figures/wave_explorer.png}
%     \caption{The \texttt{wave\_explorer} tool applied to Gl~410 (DS~Leo). The main panel shows the observed spectrum (grey) and the model fit (red), with the residuals (obs $-$ fit) in the lower sub-panel. Selected regions are drawn as shaded vertical bands, colour-coded by their mean pixel-wise $\chi^2$. Hovering over a region displays its statistics. VALD lines can be overplotted to identify which species and transition each spectral feature corresponds to. Regions can be added, excluded, or have their bounds adjusted. Summary statistics for all regions are given in the lower-left panel, while the lower-right panel lists the worst-fitted regions, each of which can be clicked to zoom directly to that region.}
%     \label{fig:wave_explorer}
% \end{figure*}

% A careful selection of absorption lines is crucial. Not all of the observed spectrum is usable, and not all of it is equally informative. Parts are degraded by low signal-to-noise or contaminated by
% telluric features, and the forward model reproduces some regions more
% faithfully than others. 
A careful selection of absorption lines is crucial. Not all of the observed spectrum is usable, and not all of it is equally informative. Some regions are reproduced far less faithfully than others, because the model atmospheres used in \texttt{ZeeTurbo} remain imperfect, and these regions are best excluded. Parts are also degraded by low signal-to-noise or telluric contamination. We therefore restrict the analysis to a curated set of spectral regions by evaluating the likelihood of Sect.~\ref{sec:methods_likelihood} over them. These regions alone then constrain the retrieved parameters. In what follows we use \emph{line list} for the set of spectral lines of interest and \emph{regions} for the wavelength windows around them on which the likelihood is computed.

% Each parameter alters a line's strength, width, or central wavelength in ways that partly mimic the others, so the list must be built to separate these responses rather than let them be absorbed into one another \citep{Kochukhov_2021}. The degeneracy between metallicity and magnetic intensification is a clear example: both raise the equivalent width of magnetically sensitive Fe lines. A transition's magnetic sensitivity is set by its effective Land\'e factor $g_\mathrm{eff}$, which fixes the size of the Zeeman splitting at a given field strength, so a list dominated by high-$g_\mathrm{eff}$ Fe transitions cannot distinguish a more metal-rich, weakly magnetic star from a less metal-rich, strongly magnetic one.

% These degeneracies set the selection criteria. The list should span a range of $g_\mathrm{eff}$, since comparing transitions of differing magnetic sensitivity is what separates Zeeman broadening from the rotational and turbulent broadening. Longer-wavelength lines are favoured, as the Zeeman splitting grows with wavelength \citep{donati_landstreet+2009, Reiners+2012}. The group of Ti~\textsc{i} lines near 970~nm identified by \citet{Kochukhov+2017, Shulyak+2017, Kochukhov_2021} is especially valuable for this reason. It is equally important to include at least one transition with negligible $g_\mathrm{eff}$, because such a line pins down the non-magnetic broadening, principally the macroturbulence $\zeta_\mathrm{RT}$ and the stellar rotation, independently of the field, disentangling the two broadening effects.

Certain parameters leave similar imprints on the spectrum, and the list must break the resulting degeneracies. The clearest case is line broadening: separating the field from rotation and macroturbulence requires lines spanning a range of $g_\mathrm{eff}$, since only the magnetic broadening depends on it (Eq.~\ref{eq:zeeman}). Longer-wavelength lines are favoured, as the splitting grows with $\lambda^2$. The Ti~\textsc{i} group near 970~nm is especially valuable here \citep{Kochukhov+2017, Shulyak_2017, Kochukhov_2021}. Including at least one transition with negligible $g_\mathrm{eff}$ anchors the non-magnetic broadening independently of the field, disentangling the two broadening effects.

To assemble and refine this list I built \texttt{wave\_explorer}, an
interactive spectrum-exploration tool. For a given star it overlays the observed spectrum on a synthetic spectrum generated from the point estimates of the parameters of a completed pipeline run, showing directly which features the forward model reproduces and which it misses. Region boundaries can be adjusted, new windows added, and existing ones removed. The tool also reports the residuals between observation and model and assigns a quality flag from the mean pixel-wise $\chi^2$. A high value marks a systematic mismatch, such as a poorly modelled line, an unmasked telluric, or a continuum error. Sorting by this flag produces a worst-first table, letting us quickly inspect where model and data disagree most and prune or remask accordingly. On top of this, lines from the VALD database are overplotted and labelled by species, indicating which transitions are responsible for each observed feature. A screenshot of the tool is displayed in Fig.~\ref{fig:wave_explorer}.

\texttt{wave\_explorer} can be run on a single star or on many at once.
Single-star mode tailors a list to one target, whereas multi-star mode builds a list that performs well across a diverse sample, as required when a common selection must serve many stars. In multi-star mode the displayed spectrum and residuals are the pixel-by-pixel averages of the observed and model flux on a common wavelength grid, which exposes the features shared across the sample. The quality flag of each region is then the median $\chi^2$ over stars, so a region is penalised only when it fits poorly for the sample as a whole rather than for a single outlier.

The curated list this process yields is what we refer to as the common line list, which comprises $29$ wavelength regions in the range $747$--$979\,\mathrm{nm}$. The species and effective Land\'e factor $g_\mathrm{eff}$ of each region are identified with the strongest VALD line falling inside the region. Table~\ref{tab:linelist_opt} reports the resulting identifications. The list is split roughly evenly between Ti\,\textsc{i} ($14$ lines) and Fe\,\textsc{i} ($13$ lines), with single Mg\,\textsc{i} and V\,\textsc{i} regions.

% \begin{table}
% \caption{Initial optical line list. The identification relied on wavelengths and depths extracted from the VALD database. Each entry reports the central wavelength of the line region (vacuum \AA). Effective Landé factors are given for atomic lines.}
% \label{tab:linelist_opt}
% \centering
% \small
% \setlength{\tabcolsep}{4pt}
% \begin{tabular}{@{}c@{\hspace{6pt}}>{\centering\arraybackslash}p{\dimexpr\linewidth-1.5cm\relax}@{}}
% \toprule
% Species & Wavelength (\AA) [$g_\mathrm{eff}$] \\
% \midrule
% Fe\,\textsc{i} & 7513.12 [1.42], 8049.86 [1.50], 8077.42 [0.76], 8222.67 [1.11], 8470.79 [2.49], 8516.35 [1.83], 8517.45 [0.78], 8584.60 [1.07], 8614.15 [1.49], 8623.90 [1.22], 8759.60 [1.49], 8807.06 [1.02], 8840.88 [1.50] \\
% Mg\,\textsc{i} & 8809.15 \\
% Ti\,\textsc{i} & 7472.04 [1.37], 8027.08 [1.06], 8070.45 [0.75], 8399.16 [0.00], 8419.26 [0.87], 8441.62 [1.12], 8677.72 [1.19], 8685.38 [0.99], 9691.50 [1.50], 9708.31 [1.26], 9721.64 [0.95], 9731.10 [1.00], 9772.98 [1.55], 9786.11 [1.48] \\
% V\,\textsc{i} & 8119.00 [1.42] \\
% \bottomrule
% \end{tabular}
% \end{table}

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{figures/region_filtering.pdf}
    \caption{The region filter in action for Gl~445. Each panel shows one spectral region: the observed spectrum (black) against the fitted model (red). A region is kept when $\chi^2_R/N_R < 50$ (left column, green) and dropped when they exceed the threshold (right column, red). See Sect.~\ref{sec:methods_filter} and Fig.~\ref{fig:linelist_filter}.}
    \label{fig:region_filtering}
\end{figure}

\subsection{Per-star line-list filter}
\label{sec:methods_filter}

% The common list of Sect.~\ref{sec:methods_linelist} is a single compromise for a diverse sample. Because the targets of Table~\ref{tab:obs_summary} span a range of M spectral types and data quality, a region that is clean and well modelled in one star can be unusable in another. Telluric absorption falls at different stellar-frame wavelengths depending on a star's radial velocity and observing epochs, so a line free of contamination in one target may be blended in another, and low S/N can leave a region uninformative. Line strengths also depend strongly on $T_\mathrm{eff}$, so a transition that is clean in a warmer M dwarf can be overwhelmed by a growing blend in a cooler one. \citet{reiners+2022} address both by tailoring the selection per star: a minimum S/N per line, a rejection of lines lying too close to tellurics, and a progressive exclusion of lines below $T_\mathrm{eff}=3750$ and $3600\,\mathrm{K}$. Rather than impose fixed temperature cuts, we identify the regions to drop empirically, from a short diagnostic run of each star.

The common list is a single compromise for a diverse sample. Because the targets of Table~\ref{tab:obs_summary} span a range of M spectral types and data quality, a region that is clean and well modelled in one star can be unusable in another. Telluric absorption falls at different stellar-frame wavelengths depending on a star's radial velocity and observing epoch, so a line free of contamination in one target may be blended in another, and low S/N can leave a region uninformative. Line strengths also depend strongly on $T_\mathrm{eff}$, so a transition that is clean in a warmer M dwarf can be overwhelmed by a growing blend in a cooler one. \citet{reiners+2022} handle this by tailoring the selection per star, including fixed $T_\mathrm{eff}$ cuts that progressively drop lines toward cooler stars. Rather than impose such cuts, we identify the regions to drop empirically, from a short diagnostic run of each star.

% \begin{figure}
% \centering
% \resizebox{\columnwidth}{!}{%
% \begin{tikzpicture}[
%   font=\small, text=black,
%   >={Stealth[length=2.6mm,width=2mm]},
%   % ---- shared geometry: every box lines up to the same grid ----
%   box/.style={draw, align=center, inner sep=2pt, minimum height=9mm,
%               line width=0.8pt, fill=white},
%   card/.style={box, text width=28mm},                  % fixed-width step box
%   term/.style={box, rounded corners=8pt, inner xsep=9pt, inner ysep=1pt, line width=0.9pt,
%                draw=termAc, fill=termBg, font=\small\bfseries},  % start / end
%   diag/.style={card, draw=diagAc},                     % diagnostic-phase step
%   prod/.style={card, draw=prodAc},                     % production-phase step
%   bridge/.style={card, rounded corners=4pt, draw=edge},% decision outcome
%   dec/.style={draw=decAc, fill=decBg, diamond, aspect=2.4,
%               align=center, inner sep=2pt, line width=0.8pt},     % decision
%   arr/.style={->, draw=edge, line width=0.8pt},
%   pno/.style={font=\footnotesize\bfseries, draw=noAc, text=noAc, fill=white,
%               line width=0.7pt, inner sep=2.5pt, rounded corners=2pt},
%   pyes/.style={font=\footnotesize\bfseries, draw=yesAc, text=yesAc, fill=white,
%                line width=0.7pt, inner sep=2.5pt, rounded corners=2pt},
%   banner/.style={font=\small\bfseries, text=white, inner sep=3.5pt,
%                  rounded corners=3pt, anchor=west, draw=diagAc},                % phase tab
% ]

% % ============================= start ===============================
% \node[term] (A) {Star $+$ common line list};

% % ========================= diagnostic spine ========================
% \node[diag, below=8mm of A] (B) {Run pipeline with reduced steps};
% \node[diag, below=6mm of B]  (C) {Compute $\chi^2_R/N_R$ for each region};
% \node[dec,  below=5mm of C]  (D) {Any region with\\[1pt]$\chi^2_R/N_R > 50$\,?};

% % ===================== branch (decision outcome) ===================
% \node[diag] (E) at ([xshift=-22mm,yshift=-15mm]D.south) {Common list is final};
% \node[diag] (F) at ([xshift= 22mm,yshift=-15mm]D.south) {Drop rejected regions\\$\rightarrow$ filtered list};

% % ======================== production lanes =========================
% \node[prod, below=12mm of E] (G) {Run to full length};
% \node[prod, below=12mm of F] (H) {Rerun from scratch\\on filtered list};
% \node[term] (I) at ($(G.south)!0.5!(H.south)+(0,-8mm)$) {Posterior estimates};

% % ===================== phase frames (background) ===================
% \coordinate[above=4mm of B] (diagTop);
% \coordinate[below=21mm of D] (diagBot);
% \coordinate[above=6mm of G] (prodTop);
% \coordinate[below=3mm of I] (prodBot);
% \begin{scope}[on background layer]
%   \node[draw=diagAc, fill=diagBg, rounded corners=9pt, line width=1pt,
%         fit=(B)(C)(D)(diagTop)(diagBot), inner xsep=23.5mm, inner ysep=0mm] (diagF) {};
%   \node[draw=prodAc, fill=prodBg, rounded corners=9pt, line width=1pt,
%         fit=(G)(H)(I)(prodTop)(prodBot), inner xsep=8mm, inner ysep=0mm] (prodF) {};
% \end{scope}

% % ============================= arrows ==============================
% \draw[arr] (A) -- (B);
% \draw[arr] (B) -- (C);
% \draw[arr] (C) -- (D);

% \draw[arr] (D.south) -- (E.north) node[pno, pos=0.5] {No};
% \draw[arr] (D.south) -- (F.north) node[pyes, pos=0.5] {Yes};

% \draw[arr] (E) -- (G);
% \draw[arr] (F) -- (H);

% \draw[arr] (G.south) |- (I.west);
% \draw[arr] (H.south) |- (I.east);

% % phase banners drawn LAST so they sit on top of the arrows; fill masks
% % any arrow passing beneath, keeping the label text uncut
% \node[banner, fill=diagAc] at ([xshift=7mm]diagF.north west) {Diagnostic phase};
% \node[banner, fill=prodAc] at ([xshift=7mm]prodF.north west) {Production phase};

% \end{tikzpicture}%
% }
% \caption{Per-star line-list filtering workflow. In the diagnostic phase (blue) the pipeline is run using the common line list with a reduced number of MCMC steps, and the mean per-pixel $\chi^2$ over the fitted pixels of each region, $\chi^2_R/N_R$, is evaluated. If no region exceeds the threshold the common list is final and the run is carried to its full production length. If any region does, those regions are filtered out and the pipeline is rerun from scratch with the filtered line list. The production phase (green) is in either case an ordinary pipeline run that differs only in the line list it uses, and it is this run that yields the posterior estimates.}
% \label{fig:linelist_filter}
% \end{figure}

% The filter runs in two phases (Fig.~\ref{fig:linelist_filter}). In the diagnostic phase the star is fitted on the full common list with a reduced number of MCMC steps, the aim being a representative synthetic spectrum rather than a converged posterior. The forward model evaluated at the point estimates of this posterior gives this synthetic spectrum, which we compare to the observations region by region. For a region $R$ we take the mean per-pixel $\chi^2$ over its $N_R$ fitted pixels ($\chi^2_R/N_R$). This is also the quantity reported by the tool used to build the list by hand (Sect.~\ref{sec:methods_linelist}), so manual and automatic selection rank regions using a common method. Because the diagnostic synthetic spectrum depends on the model configuration of the diagnostic fit, the filtered list inherits that dependence, so runs that adopt different magnetic configurations can retain slightly different sets of regions.

The filter runs in two phases (Fig.~\ref{fig:linelist_filter}). In the diagnostic phase the star is fitted on the full common list with a reduced number of MCMC steps (2000). The aim is a representative fit rather than a converged posterior. Evaluating the forward model at the best-fit parameters yields the diagnostic spectrum, which we compare to the observations region by region. For a region $R$ we take the mean per-pixel $\chi^2$ over its $N_R$ fitted pixels ($\chi^2_R/N_R$). This is the same quantity reported by the tool used to build the list by hand (Sect.~\ref{sec:methods_linelist}), so manual and automatic selection rank regions on a common footing. 
% This fit assumes a magnetic configuration --- the number of field components and their filling factors --- which shapes its point estimates and hence the diagnostic spectrum built from them. The filtered list inherits this dependence: two runs that assume different configurations can retain slightly different regions.

A region is rejected when $\chi^2_R/N_R > 50$, a threshold set by inspecting the per-region model. Below the threshold the model reproduces the data well, while higher values mark mismatches such as poorly modelled line, a residual telluric, or a continuum error. Since the metric is a mean of squared, error-normalised residuals, $50$ corresponds to a mean per-pixel residual of $\sim\!7\sigma$, so the cut is deliberately permissive, leaving marginal regions in the fit.

% Figure~\ref{fig:region_filtering} shows the filter at work for Gl~445, putting the diagnostic phase of Fig.~\ref{fig:linelist_filter} into practice. Each panel displays the observed spectrum against the model fitted to it over one region of the common line list, with the grey band marking the pixels that enter $\chi^2_R/N_R$ and the value itself printed in the bottom-right corner, in green when the region is kept and red where it is dropped. The kept regions in the left column track the model closely and sit below the threshold. The rejected regions in the right column fall in the $967$--$979\,\mathrm{nm}$ band, where telluric absorption pulls it away from the data and sends $\chi^2_R/N_R$ over the threshold.

Figure~\ref{fig:region_filtering} shows the diagnostic phase (Fig.~\ref{fig:linelist_filter}) at work on Gl~445. Each panel shows one region of the common line list, with the observed spectrum and the fitted model. The grey band marks the pixels entering $\chi^2_R/N_R$, and the value is printed in the bottom-right corner --- green where the region is kept, red where it is rejected. The kept regions, in the left column, track the model closely and fall below the threshold. The rejected regions, in the right column, lie in the $967$--$979\,\mathrm{nm}$ band, where telluric absorption pulls the model away from the observations and pushes $\chi^2_R/N_R$ over the threshold.

% The diagnostics set the production phase, which is the full-length retrieval that yields the reported parameters --- the same pipeline of Sect.~\ref{sec:methods_fit}, run to the full number of MCMC steps (5000) rather than the reduced count of the diagnostic phase (2000). If no region is rejected, the common list is final and the run continues to the full production length. If any region is rejected, the star is refitted from scratch on its filtered list. The filtered list is saved as a copy of the common list with rejected regions commented out and annotated with their $\chi^2_R/N_R$, keeping a record of what was dropped for each star. Either way, the production run differs from a standard fit only in the line list it uses, and it is this run that yields the reported parameters.

The production phase is the full-length retrieval that yields the reported parameters: the same pipeline as Sect.~\ref{sec:methods_fit} run for the full 5000 MCMC steps rather than the 2000 of the diagnostic phase. How it is launched depends on the diagnostic outcome. If no region is rejected, the common list is already final and the run simply continues to the full production length. If any region is rejected, the star is refitted from scratch on a filtered list --- a copy of the common list with the rejected regions commented out and annotated with their $\chi^2_R/N_R$, recording what was dropped for each star.


\subsection{Magnetic-component selection via BIC}
\label{sec:methods_bic}
% The magnetic model is described in Sect.~\ref{sec:methods_magnetic}. Its filling factors are constrained to be non-negative ($f_i \geq 0$). For weakly magnetic stars this introduces an asymmetry: the filling factors of the strong-field components cannot scatter below zero, so noise pushes their posterior means to small positive values rather than averaging out around zero. Because the mean field $\langle B \rangle = \sum_i B_i f_i$ weights each component by its field strength, these spurious filling factors are amplified by the large $B_i$ of the strong-field components, biasing $\langle B \rangle$ upward. The full magnetic grid therefore overestimates $\langle B \rangle$ for weakly magnetic stars, and we reduce the number of components carried as free parameters to recover a lower, less biased estimate.

The magnetic model is described in Sect.~\ref{sec:methods_magnetic}. Whenever a component's true filling factor lies near zero --- the lower bound of the prior --- its posterior is truncated there and its mean is displaced to a small positive value rather than averaging to zero. This boundary effect occurs in weakly and strongly magnetic stars alike, but its impact on the mean field depends on which components are affected, since each field strength is weighted by its filling factor. In a strongly magnetic star the dominant high-field components carry a genuine, well-constrained filling factor and $\langle B \rangle$ is large, so the residual bias from any near-bound components is a small fractional effect. In a weakly magnetic star, it is the high-field components that sit near zero. Each one still picks up a small spurious filling factor, and multiplying that by a large $B_i$ turns it into a sizeable $B_i f_i$ term. The true $\langle B \rangle$ is small, so these few spurious terms can make up a significant fraction of the sum and push it upward. Therefore, as in \citet{Lavail_2019}, we use the BIC to limit the number of components carried as free parameters, dropping high-field components the data do not require.

We select the number of magnetic components per star with the Bayesian information criterion \citep{Schwarz+1978, Sharma_2017},
\begin{equation}
    \label{eq:bic}
    \mathrm{BIC} = -2 \ln \mathcal{L} + k\,\ln n,
\end{equation}
where $\mathcal{L}$ is the likelihood (Eq.~\ref{eq:likelihood}) evaluated at the production best-fit (maximum-likelihood) parameters, $k$ is the number of free parameters, and $n$ is the number of fitted spectral pixels. The first term rewards goodness of fit, while the penalty $k\ln n$ grows with model complexity, so an additional component is justified only when it improves the likelihood enough to outweigh the penalty. The configuration with the lowest BIC is preferred. Locating this minimum directly would require a separate MCMC run for each number of components, which is very expensive across the full sample. We therefore approximate the BIC with a rapid \emph{scan}.

% We apply this post hoc, without re-running the MCMC. The production run (Sect.~\ref{sec:methods_filter}) uses a magnetic grid spanning $0$--$10\,\mathrm{kG}$ in $1\,\mathrm{kG}$ steps. From its best-fit solution we compute the BIC of the full configuration and then collapse the grid from the top down. At each step the strongest component is removed and its filling factor transferred to the next-strongest component, the synthetic spectrum is recomputed with all other parameters held at their best-fit values, and the likelihood and BIC are re-evaluated, lowering $k$ by one. Transferring the filling factor to the adjacent bin rather than discarding it lowers $\langle B \rangle$ by only $f_{\rm drop}\,\Delta B$ per step (with $\Delta B = 1\,\mathrm{kG}$ the grid spacing), so the collapse traces a smooth, monotonically decreasing sequence in $\langle B \rangle$. We repeat until only the non-magnetic component remains and adopt the lowest-BIC configuration. The chosen configuration is then rerun in the production phase with the star's filtered line list from the diagnostic phase, from which the reported $\langle B \rangle$ is derived. Figure~\ref{fig:bic_scan_ds_leo} illustrates this scan for Gl~410, where the BIC turns over once the unsupported high-field components have been stripped. Because removing a genuine strong-field component sharply degrades the fit while removing a spurious one barely changes it, the criterion is self-regulating.

% We perform this scan after the production phase (Sect.~\ref{sec:methods_filter}), evaluating each candidate post hoc without further MCMC sampling. Starting from the production best-fit solution we compute its BIC, then collapse the grid from the high-field end downward. At each step the strongest remaining component is removed and its filling factor added to the next component down, keeping the filling factors normalised to unity ($\sum_i f_i = 1$); discarding it would break that constraint. The synthetic spectrum is recomputed with all other parameters held at their best-fit values, and the likelihood and BIC are re-evaluated with $k$ reduced by one. Because the transferred flux moves down a single bin, each step lowers $\langle B \rangle$ by $f_{\rm drop}\,\Delta B$ (with $\Delta B = 1\,\mathrm{kG}$), so the collapse traces a smooth, monotonically decreasing sequence in $\langle B \rangle$. We repeat until only the non-magnetic component remains and adopt the configuration with the lowest BIC. Because the remaining parameters are held fixed rather than re-optimised at each step, this is an approximation to the BIC, exact in the limit where the removed components carry negligible filling. Figure~\ref{fig:bic_scan_validation} validates it against a reference that runs a separate MCMC fit at each configuration for both Gl~410 and Gl~725~A. The scan and the reference reach their minimum at the same field strength, so the single-run scan recovers the same optimum at a fraction of the cost.

We perform this scan after the production phase (Sect.~\ref{sec:methods_filter}), evaluating each candidate post hoc without further MCMC sampling. We compute the BIC of the best-fit solution, then collapse the field grid downward, starting from the highest-field component. At each step we remove the strongest remaining component and transfer its filling factor $f_{\rm drop}$ to the next component down, keeping the filling factors normalised to unity ($\sum_i f_i = 1$). We then recompute the synthetic spectrum with all other parameters held at their best-fit values and re-evaluate the likelihood and BIC with $k$ reduced by one. Since the transferred flux moves down a single $\Delta B = 1\,\mathrm{kG}$ bin, each step lowers $\langle B \rangle$ by $f_{\rm drop}\,\Delta B$, so the collapse traces a smooth, monotonically decreasing sequence in $\langle B \rangle$. We continue until only the non-magnetic component remains and adopt the configuration with the lowest BIC.

Because the other parameters are held fixed rather than re-optimised, this is an approximation to the BIC. Figure~\ref{fig:bic_scan_validation} validates this approximation for Gl~410 and Gl~725~A against a reference that runs a separate MCMC fit at each configuration: both reach their minimum at the same field strength, so the single-run scan recovers the same optimum at a fraction of the cost.

% The scan operates on the error-inflated likelihood of Sect.~\ref{sec:methods_likelihood}, so a large inflation factor could in principle make components cheaper to remove. We checked this across the sample and found no correlation between the first-pass $\chi^2_\nu$ and the number of stripped components, so component removal tracks the field rather than the fidelity of the model fit.
% % TODO(Dion): optionally add scatter figure chi2nu vs stripped components

% For three stars spanning the activity range (Gl~410, Gl~514, Gl~725A) we reran the full MCMC at every component configuration visited by the scan. The resulting BIC curves, and in particular their minima, coincided with those of the frozen-parameter scan, confirming that holding the non-magnetic parameters fixed during the collapse does not bias the selection.
% PENDING: figure to be produced; injection-recovery suite extends this to the fully-stripped regime

% \begin{figure}
%     \centering
%     \includegraphics[width=\linewidth]{figures/bic_scan_ds_leo.pdf}
%     \caption{The post-hoc BIC scan for Gl~410. Plotted is $\Delta\mathrm{BIC}$, the BIC relative to its lowest value, against the strongest magnetic component still retained as the scan collapses the $0$--$10\,\mathrm{kG}$ grid from the top down. The curve falls as unsupported high-field components are stripped and rises once a genuine one is removed. The minimum (red point) is adopted: five components with a maximum field of $4\,\mathrm{kG}$. The inset magnifies the shallow upturn beyond the minimum, with the dotted line at $\Delta\mathrm{BIC}=10$ for reference.}
%     \label{fig:bic_scan_ds_leo}
% \end{figure}

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{figures/bic_optimal_summary_absolute.pdf}}{%
    \caption{BIC versus the maximum magnetic field component allowed in the fit, for GL~410 and GL~725~A. The BIC scan (solid black) derives every point from a single MCMC run by removing the strongest components one at a time, while the reference (dashed blue) runs a separate MCMC fit at each configuration. Both reach their minimum at the same field strength — 4~kG for GL~410 and 6~kG for GL~725~A (red marker, dashed line) — so the single-run scan recovers the same optimal configuration as the far more expensive per-configuration runs.}
    \label{fig:bic_scan_validation}
\end{figure}

% \subsection{Validation on synthetic spectra}
% \label{sec:methods_injection}
% To characterise the pipeline's response under controlled conditions, we generate synthetic observations from the forward model at known parameters and mean fields $\langle B \rangle \in \{0, 0.3, 0.5, 1, 2, 4\}\,\mathrm{kG}$. Each synthetic spectrum is degraded with Gaussian noise matched to two representative signal-to-noise ratios bracketing the sample, and then pushed through the complete pipeline, including the per-star line-list filter (Sect.~\ref{sec:methods_filter}) and the BIC scan (Sect.~\ref{sec:methods_bic}). Comparing the recovered parameters with the injected truth tests whether the procedure returns unbiased atmospheric parameters and an accurate $\langle B \rangle$ across the activity range, and in particular how it behaves for the weakly magnetic stars whose high-field components the scan strips.
% % PENDING: results from injection-recovery suite — recovery figure, detection floor, upper-limit definition for stripped stars

