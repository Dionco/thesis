# Waivers

Author overrides, each with **explicit scope**. Never re-flag a waived pattern within its
scope. A waiver out of scope is not a waiver (a §3.2-scoped passive-voice waiver does not
protect passives in §3.4). Format:

```
- **W-001** (scope: §X only | global): <pattern> — <one-line rationale>. [session NNNN]
```

_(No waivers yet. Patterns the author overrides during review land here.)_

## Standing terminology (from CONTEXT.md — treat as pre-waived; flag drift FROM these)
These are the author's controlled terms; using them is correct, and prose that drifts from
them is itself a finding (Pass 5). Not re-flag the preferred form as wrong:
- `asap` (lower-case), never "ASAP" / "Asap".
- "the sample" = Cristofari sample ∪ new-targets campaign; use the specific name when the
  distinction matters.
- "self-consistent" = homogeneous pipeline application — **not** optical/NIR agreement
  (that is "cross-instrument consistency").
- "magnetic component" (not "bin" / "field component"); "filling factor" (not "covering
  fraction"); "χ²/N per region" / "mean pointwise χ²" (never "reduced χ²" for the filter).
- "the ZeeTurbo grid" / "Cristofari's grid", never "our grid".
